export const selectRooms = (state) => state.room.allRooms;

export const selectCurrentRoom = (state) => state.room.currentRoom;

export const selectCurrentRoomData = (state) => state.room.roomData;

export const selectRoomMessages = (state) => state.room.messages;
